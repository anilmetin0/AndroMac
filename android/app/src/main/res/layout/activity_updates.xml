<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/scrollRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    android:scrollbarStyle="outsideOverlay"
    android:clipToPadding="false">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingBottom="32dp">

        <include layout="@layout/view_header" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:paddingTop="12dp">


        <TextView style="@style/SectionLabel" android:text="@string/update_section_check" />

        <LinearLayout style="@style/Card">

            <LinearLayout android:id="@+id/rowCheck" style="@style/Row">
                <LinearLayout
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:orientation="vertical">
                    <TextView style="@style/RowTitle" android:text="@string/update_check" />
                    <TextView style="@style/RowSubtitle" android:text="@string/update_check_note" />
                </LinearLayout>
                <Switch android:id="@+id/swCheck" style="@style/RowSwitch" />
            </LinearLayout>

            <LinearLayout android:id="@+id/rowAuto" style="@style/Row">
                <LinearLayout
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:orientation="vertical">
                    <TextView style="@style/RowTitle" android:text="@string/update_auto" />
                    <TextView style="@style/RowSubtitle" android:text="@string/update_ready" />
                </LinearLayout>
                <Switch android:id="@+id/swAuto" style="@style/RowSwitch" />
            </LinearLayout>

            <LinearLayout android:id="@+id/rowBeta" style="@style/Row">
                <TextView
                    style="@style/RowTitle"
                    android:layout_width="0dp"
                    android:layout_weight="1"
                    android:text="@string/update_beta" />
                <Switch android:id="@+id/swBeta" style="@style/RowSwitch" />
            </LinearLayout>

        </LinearLayout>

        <LinearLayout style="@style/Card" android:paddingTop="16dp" android:paddingBottom="16dp">

            <!-- One line: what the install is doing or why it stopped, else what the last check found. -->
            <TextView
                android:id="@+id/checkSummary"
                style="@style/RowSubtitle"
                android:paddingStart="16dp"
                android:paddingEnd="16dp"
                android:accessibilityLiveRegion="polite" />

            <ProgressBar
                android:id="@+id/progress"
                style="?android:attr/progressBarStyleHorizontal"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginStart="16dp"
                android:layout_marginEnd="16dp"
                android:layout_marginTop="8dp"
                android:max="100"
                android:visibility="gone" />

            <!-- The notes of the newer build, English or Turkish, from the release body. -->
            <TextView
                android:id="@+id/notes"
                style="@style/RowSubtitle"
                android:paddingStart="16dp"
                android:paddingEnd="16dp"
                android:paddingTop="8dp"
                android:textIsSelectable="true"
                android:visibility="gone" />

            <!-- The one button: Check for updates, Download and install, Install now or Retry (UpdateCheck.action). -->
            <Button
                android:id="@+id/download"
                style="@style/PrimaryButton"
                android:layout_marginTop="12dp"
                android:layout_marginBottom="-4dp" />
        </LinearLayout>

        </LinearLayout>
    </LinearLayout>
</ScrollView>
